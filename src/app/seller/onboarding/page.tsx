import { OnboardingClient } from "./OnboardingClient";

export const metadata = { title: "Đăng ký bán hàng | MMO Market" };

export default function OnboardingPage() {
  return <OnboardingClient />;
}
